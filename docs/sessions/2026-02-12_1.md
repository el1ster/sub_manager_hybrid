# Сесія 2026-02-12_1

## 🏁 Що зроблено:
1. **Ініціалізація проєкту**:
   - Створено структуру папок `src/`, `docs/`, `tests/`.
   - Налаштовано Git, авторство закріплено за `elister <dennisandmia2@gmail.com>`.
   - Проєкт опубліковано на GitHub: `https://github.com/el1ster/sub_manager_hybrid`.

2. **База даних**:
   - Реалізовано ORM моделі SQLAlchemy в `src/core/models.py`, що повністю відповідають вашому SQL-скрипту.
   - Створено `src/database/db_manager.py` з автоматичною ініціалізацією БД `sub_manager.sqlite`.
   - Реалізовано початкове заповнення (Seed): категорії, валюта UAH.

3. **Безпека та Конфігурація**:
   - `src/core/security.py`: Реалізація AES-256 (Fernet) для шифрування обміну з ботом.
   - `src/core/config.py`: Централізоване керування налаштуваннями через `.env`.
   - Автоматична генерація та збереження в БД ключа `enc_key`.

4. **Інтерфейс**:
   - `src/ui/main_window.py`: Каркас PySide6 з 4-ма вкладками ([Управління], [Статистика], [Історія], [Налаштування]).
   - Мова інтерфейсу — українська.
   - Стилі QSS тимчасово очищені (будуть пророблятися пізніше).

## 📍 Поточний стан:
- Система успішно проходить "Smoke test" (ініціалізація та перевірка ключів).
- Код зафіксовано в GitHub.

## 🚀 План на наступну сесію:
1. Наповнення вкладки **[Управління]**: створення таблиці підписок (`QTableView`) та бокової панелі для модерації чернеток (`Drafts`).
2. Реалізація методів CRUD (створення, читання, оновлення, видалення) для підписок у репозиторії.
